{
  "name": "playwright",
  "description": "DEPRECATED: Use playwright-cli instead (npm install -g @playwright/cli@latest). Browser automation via Playwright MCP - kept for backward compatibility.",
  "command": "npx",
  "args": ["@playwright/mcp@latest", "--isolated", "--save-video=1920x1080", "--viewport-size=1920x1080"],
  "env": {},
  "prerequisites": ["node >= 18"],
  "post_install": ["npx playwright install chromium"],
  "dirs_to_create": ["videos", "outputs/e2e"],
  "gitignore_entries": ["videos/", "outputs/", "*.webm"]
}
