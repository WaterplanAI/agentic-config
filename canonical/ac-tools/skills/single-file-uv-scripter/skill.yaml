skill_id: single-file-uv-scripter
kind: standard
summary: 'Creates self-contained Python scripts with inline PEP 723 metadata for UV.
  Embeds dependencies directly in script headers for zero-config execution via `uv
  run`. Triggers on keywords: uv script, single file script, inline dependencies,
  PEP 723, self-contained python, uv run script, standalone script'
project_agnostic: true
body_file: body.md
assets: []
attachment_refs: []
renders:
  claude:
    status: supported
    output_path: skills/single-file-uv-scripter/SKILL.md
    display_name: single-file-uv-scripter
    allowed_tools:
    - Read
    - Write
    - Edit
    - Bash
    frontmatter_enabled: true
    description: 'Creates self-contained Python scripts with inline PEP 723 metadata
      for UV. Embeds dependencies directly in script headers for zero-config execution
      via `uv run`. Triggers on keywords: uv script, single file script, inline dependencies,
      PEP 723, self-contained python, uv run script, standalone script'
  pi:
    status: supported
    output_path: skills/ac-tools-single-file-uv-scripter/SKILL.md
    display_name: ac-tools-single-file-uv-scripter
    allowed_tools:
    - Read
    - Write
    - Edit
    - Bash
    frontmatter_enabled: true
    description: 'Creates self-contained Python scripts with inline PEP 723 metadata
      for UV. Embeds dependencies directly in script headers for zero-config execution
      via `uv run`. Triggers on keywords: uv script, single file script, inline dependencies,
      PEP 723, self-contained python, uv run script, standalone script'
