skill_id: gh-assets-branch-mgmt
kind: standard
summary: 'Manages GitHub assets branch for persistent image hosting in PRs. Creates
  orphan branch, uploads files, generates raw URLs. Bypasses transient CDN tokens.
  Triggers on keywords: assets branch, upload screenshots, pr images, persistent images,
  github assets'
project_agnostic: true
body_file: body.md
assets:
- asset_id: bundled-skill-files
  source: assets
  outputs:
    claude: .
    pi: .
attachment_refs: []
renders:
  claude:
    status: supported
    output_path: skills/gh-assets-branch-mgmt/SKILL.md
    display_name: gh-assets-branch-mgmt
    allowed_tools:
    - Bash
    - Read
    - Glob
    - Grep
    frontmatter_enabled: true
    description: 'Manages GitHub assets branch for persistent image hosting in PRs.
      Creates orphan branch, uploads files, generates raw URLs. Bypasses transient
      CDN tokens. Triggers on keywords: assets branch, upload screenshots, pr images,
      persistent images, github assets'
  pi:
    status: supported
    output_path: skills/ac-git-gh-assets-branch-mgmt/SKILL.md
    display_name: ac-git-gh-assets-branch-mgmt
    allowed_tools:
    - Bash
    - Read
    - Glob
    - Grep
    frontmatter_enabled: true
    description: 'Manages GitHub assets branch for persistent image hosting in PRs.
      Creates orphan branch, uploads files, generates raw URLs. Bypasses transient
      CDN tokens. Triggers on keywords: assets branch, upload screenshots, pr images,
      persistent images, github assets'
    body_file: body.pi.md
