# skills conventions

Later phases add exported pi skills here as `SKILL.md` folders with namespaced identifiers following `ac-tools-<resource>`.

Do not place top-level markdown files in `skills/`; use skill directories so the package root does not imply shipped wrapper coverage before implementation lands.
