# extensions

Place exported pi extensions in this directory.

This directory is currently documentation-only. Add real `.ts` or `.js` extension entries here only when this package exports package-local pi extensions.

Shared compat extensions for `AskUserQuestion` still ship through `node_modules/@agentic-config/pi-compat/extensions` as wired by `packages/pi-ac-qa/package.json`.
