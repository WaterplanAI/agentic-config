# config assets

This directory contains bundled default configuration files used by `@agentic-config/pi-ac-audit`.

Current shipped surface:
- `audit.default.yaml` for the package-local audit configuration skill and bundled audit hook parity
