# ac-audit default configuration
# Override at: project audit.yaml > ~/.claude/audit.yaml > this file

log_dir: "~/.claude/audit-logs"
log_permissions: 384  # 0o600 in decimal
max_words: 50

# Tools that display systemMessage in the host UI
display_tools:
  - "Bash"
