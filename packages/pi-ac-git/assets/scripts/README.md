# script assets

This directory contains bundled helper and hook scripts used by `@agentic-config/pi-ac-git`.

Current shipped surface:
- helper scripts for the current generated `ac-git` skill surface
- `hooks/git-commit-guard.py` for package-level commit-guard parity through `@agentic-config/pi-compat`
