# config assets

This directory contains bundled default configuration files used by `@agentic-config/pi-ac-safety`.

Current shipped surface:
- `safety.default.yaml` for the package-local safety configuration skill and bundled guardian hook parity
