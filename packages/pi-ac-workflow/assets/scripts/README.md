# script assets

Store copied shared helper scripts here when later phases port the source plugin assets into this pi package.

Use package-level script copies for helpers shared across multiple wrappers or adapter surfaces.
